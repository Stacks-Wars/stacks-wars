# stacks_wars_core

Portable traits, wire types, and helpers for Stacks Wars game engine crates.

Game crates should depend on this package from [crates.io](https://crates.io/crates/stacks_wars_core) and implement `GameEngine` plus a `create_*` factory with signature `GameFactory`.
