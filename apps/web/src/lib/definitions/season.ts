export interface Season {
	id: number;
	name: string;
	description?: string;
	startDate: string;
	endDate: string;
	createdAt: string;
}

export interface CreateSeasonRequest {
	name: string;
	description?: string;
	startDate: string;
	endDate: string;
}

export interface CreateSeasonRequest {
	name: string;
	description?: string;
	startDate: string;
	endDate: string;
}
